package:
  name: jivas/switchboard_interact_action
  author: V75 Inc.
  archetype: SwitchboardInteractAction
  version: 0.2.0
  meta:
    title: Switchboard Interact Action
    description: Manages and Routes users to their subscribed agents, ensuring smooth transitions and consistent communication.
    type: interact_action
    group: core
  config:
    singleton: true
    order:
      weight: -10 # Example: High priority (lower number means earlier execution)
      before: jivas/persona_interact_action # Ensure it runs before the persona interact action
  dependencies:
    jivas: ~2.1.0